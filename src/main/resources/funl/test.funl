def from( n ) =
	println( 'from ' )
	1 # from( 2 )

print( from(1) )

//   val (n, len) = maxBy( snd, (i, hailstone( i ).length()) | i <- 1:100000 )
// 
//   println( n, len )
	
	
// def f(a) =
// 	def g(b) =
// 		if b == 0
// 			b
// 		else
// 			g( b - 1 )
// 
// 	g(a)
// 
// println( f(3) )