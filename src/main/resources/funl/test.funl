def primes =
	d = dict()
	q = 3

	def oddprimes =
		for
			p1 = d.>remove( q )

			if p1 != None
				p = p1.get()
				x = p + q
				while x in d do x += p
				d(x) = p
				q += 2
			else
				d(q*q) = 2*q
				p = q
				q += 2
				return p # oddprimes()

	2 # oddprimes()

println( primes() )